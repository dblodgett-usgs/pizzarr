% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta.R
\name{create_v3_array_meta}
\alias{create_v3_array_meta}
\title{Create a V3 array metadata list.
Parallel to create_zarray_meta() for V2. Builds the zarr.json metadata
for a V3 array node. All scalar values are wrapped with jsonlite::unbox()
so the result can be encoded with toJSON(auto_unbox = FALSE).}
\usage{
create_v3_array_meta(
  shape,
  chunks,
  dtype,
  compressor,
  fill_value,
  order,
  filters,
  chunk_key_encoding = NULL,
  attributes = NULL,
  dimension_names = NULL
)
}
\arguments{
\item{shape}{Integer vector. Array shape.}

\item{chunks}{Integer vector. Chunk shape.}

\item{dtype}{Character. V2-style dtype string (e.g., "<f8", "|b1").}

\item{compressor}{Codec object or NA.}

\item{fill_value}{Fill value (numeric, logical, or NA).}

\item{order}{Character. "C" or "F" (unused in V3 codec pipeline but stored).}

\item{filters}{List of Codec objects or NA.}

\item{chunk_key_encoding}{List or NULL. If NULL, defaults to default "/" encoding.}

\item{attributes}{List or NULL. Embedded attributes. Default empty.}

\item{dimension_names}{Character vector or NULL. Omitted from zarr.json if NULL.}
}
\value{
A list ready for jsonlite::toJSON encoding.
}
\description{
Create a V3 array metadata list.
Parallel to create_zarray_meta() for V2. Builds the zarr.json metadata
for a V3 array node. All scalar values are wrapped with jsonlite::unbox()
so the result can be encoded with toJSON(auto_unbox = FALSE).
}
\keyword{internal}
